contract3c34543523 (wrong length)
docnum45c.345c (wrong length) (contains forbidden symbols)
q3c6tq234c (wrong length) (must begin with "docnum" or "contract")
docnum34y6w43yc3 (wrong length)
 (wrong length) (empty) (must begin with "docnum" or "contract")
contract r23d 2 (contains forbidden symbols)
contract=edsd23 (contains forbidden symbols)
docnumj/3d23323 (contains forbidden symbols)
contract23.er23 (contains forbidden symbols)
contrct23.er23d (contains forbidden symbols) (must begin with "docnum" or "contract")
documj/3d23323f4 (wrong length) (contains forbidden symbols) (must begin with "docnum" or "contract")
docnum (wrong length)
contract (wrong length)
